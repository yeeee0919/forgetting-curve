# PRD - 遺忘曲線背單字

---

# 產品需求文件 (PRD)：MemoFlip (暫名)

**版本：** v1.2
**核心理念：** 讓「隨手記」無縫變成「專業記憶」；讓「被動聆聽」升級為「主動解剖」。

---

## 目錄 (Table of Contents)

1. [產品定位與目標](#1-產品定位與目標)
2. [功能模組細分](#2-功能模組細分)
   - A. AI 對話式導入 (Smart Inbox)
   - B. 核心複習引擎 (SRS Engine)
   - C. 跨平台同步系統
   - D. 三層緩衝架構與認知負荷管理
   - **E. 精聽實驗室 (Listening Lab)** ← 新增
3. [使用者故事](#3-使用者故事-user-stories)
4. [技術架構建議](#4-技術架構建議)
5. [優劣勢與風險](#5-優劣勢與風險-swot)

---

---

## 1. 產品定位與目標

* **解決痛點：** Anki 介面過於複雜、選單繁瑣（如右鍵選單多達 20 個選項），且手動建立卡片耗時。
* **核心價值：** 用戶只需「貼上文字」，AI 負責結構化、去重與美化；複習時保留 Anki 精準的四階回饋邏輯。

---

## 2. 功能模組細分

### A. AI 對話式導入 (Smart Inbox)

* **功能：** 提供一個單一對話框，支援直接貼上備忘錄內容。
* **AI 處理邏輯：**
* **解析：** 自動辨識 `原文` 與 `譯文`（例如：截圖中的 "Ga slapen" 與 "Go to sleep"）。
* **去重機制：** 系統會掃描現有資料庫，若單字已存在，AI 會詢問：「此單字已在你的『荷蘭語』牌組中，要更新例句嗎？」
* **豐富化：** 自動補齊音標、真人語音 (TTS) 與一個符合當下語境的例句。



### B. 核心複習引擎 (SRS Engine)

* **演算法：** 採用改良版 FSRS 或 SM-2 演算法。
* **互動設計（參考截圖介面）：**
* **重來 (Again)：** 遺忘時使用，< 1分鐘內再次出現。
* **困難 (Hard)：** 勉強想起，< 6分鐘內再次出現。
* **良好 (Good)：** 正常記憶，< 10分鐘內再次出現。
* **簡單 (Easy)：** 熟練記憶，直接跨度到數天後。


* **視覺反饋：** 按鈕上方需即時顯示下次複習的預計時間（如：1分、10分、3天）。

### C. 跨平台同步系統

* **雲端同步：** 確保 Web、iOS、Android 端的學習進度、卡片新增/修改完全一致。
* **媒體同步：** 音訊檔（語音播放）需輕量化處理，確保同步不卡頓。

### D. 三層緩衝架構與認知負荷管理 (Cognitive Load & Inflow Control)

* **產品核心邏輯：斷點管理與漏斗控制**
  * **痛點：** 面對極大量（如 200+ 甚至數千）的待複習單字極易產生「認知疲勞」(Cognitive Fatigue) 與「分散感」。
  * **對策：** 引入「漏斗控制」與「自適應負荷」，將大型複習債務拆解為小額還款，確保大腦處於最佳心流狀態。

* **三層緩衝架構 (Three-Layer Architecture)**
  1. **總量池 (The Pool)：** 靜態儲存區，存儲所有尚未學習的 raw data 與新單字。
  2. **背誦區 (Learning Buffer)：** 
     * **限制：** 嚴格鎖定在 100 個名額（動態容量上限）。
     * **狀態：** 包含「學習中」與「重學中」的卡片。這些是正在運作「遺忘曲線」的活躍對象。
     * **出口 (畢業)：** 當在背誦區點擊「記得了」，且新間隔 (Interval) $I_{new} \ge 3$ 天時，視為「畢業」，移往熟練區，同時空出名額由總量池遞補。
  3. **熟練區 (Mastered Zone)：** 存儲所有已畢業單字，純粹在背景跑 SM-2 長期複習邏輯。

* **30 字分段複習系統 (Session-Based Review)**
  * **任務封裝：** 不論待複習總數有多少，系統每次只呈現 30 張卡片作為一組 Session。
  * **視覺回饋：** 介面頂部顯示 30 格點狀進度條，隱藏令人焦慮的剩餘總數。
  * **Session 排序 (優先權填充)：** 系統用遺忘曲線的「緊迫度」填滿這 30 個位置，排隊順序為：
    * **P0 (已背熟區)：** 根據 SM-2 計算，今天到期的舊字 (目的：防止遺忘)。
    * **P1 (背誦區 100)：** 剛按過「完全忘了」或「模糊」的字 (目的：鞏固短期記憶)。
    * **P2 (背誦區 100)：** 學習階段中，時間到期的字 (目的：推進學習進度)。
    * **P3 (總量池)：** 補充新字，直到背誦區滿 100 為止 (目的：開拓新詞彙)。

* **自適應負荷管理 (Dynamic Capacity)**
  * **動態容量：** 根據表現動態調整 Batch 大小。若使用者連續 3 組 Session 正確率皆 > 90%，詢問是否將「背誦區」從 100 擴大到 120。這代表大腦緩衝區 (RAM) 變大，可同時處理更多正在磨合的字。
  * **可預期的成就感與優勢：** 
    1. **解決分散感：** 即使有 5000 詞，大腦在「這幾天」內只會跟特定的 100 個活躍單字打交道，產生真正的「熟悉感」。
    2. **不漏掉舊知：** 熟練區的舊字時間到了必定以 P0 優先跳出。
    3. **解鎖成就：** 你會明確看到「背誦區 85/100」，知道再送走 15 個字就能解鎖新詞。

---

## 3. 使用者故事 (User Stories)

1. **導入：** 作為用戶，我可以直接把 LINE 或備忘錄裡的單字清單貼進 App，不需手動填寫 Front/Back 欄位。
2. **去重：** 作為用戶，我不希望重複背誦已經學過的字，系統應在導入時自動過濾。
3. **複習：** 作為用戶，我希望複習介面極簡，只需專注於單字本身與錄音播放。

---

## 4. 技術架構建議

| 組件 | 推薦方案 | 說明 |
| --- | --- | --- |
| **前端** | Flutter / React Native | 達成跨平台一致體驗，支援播放音訊。 |
| **AI 引擎** | GPT-4o-mini | 高速處理文本解析，成本較低。 |
| **資料庫** | Supabase (Postgres) | 支援全文檢索與向量比對，實現高效去重。 |
| **語音** | Edge TTS | 產出如截圖中播放按鈕所示的高品質語音。 |

---

## 5. 優劣勢與風險 (SWOT)

* **優勢 (Strengths)：** * 將 Anki 的複雜功能「隱藏」在對話背後。
* 大幅降低卡片製作門檻。


* **風險 (Risks)：** * **API 成本：** AI 處理大量文字會產生持續成本。
* **演算法門檻：** 需確保同步邏輯在高頻率複習下不發生資料衝突。



---

## E. 精聽實驗室 (Listening Lab) ← 新功能模組

---

# 功能模組 E：精聽實驗室 (Listening Lab) - PRD

## E.1 功能概述 (Feature Overview)

* **目標**：作為背單字功能的延伸，透過影片情境與「音素級」拆解，訓練用戶在真實語流中辨識荷蘭語單字與句型的能力。
* **入口**：原 App 主導航欄中新增「精聽實驗室」分頁。
* **核心邏輯**：**盲聽捕捉 → 點擊解剖 → 主觀評定**。
* **學習哲學**：低阻力（無測驗）、高反饋（點擊即發音）、專家導向（發音氣泡）。

---

## E.2 用戶體驗與交互設計 (UX Design)

### 第一階段：盲聽情境 (Blind Observation)
* **畫面表現**：影片（自研/自傳 MP4）自動循環播放該卡片段落（約 10-15 秒）。
* **字幕狀態**：字幕區域完全空白或僅顯示佔位符，強制用戶觀察影片畫面（如說話者神情、場景）並專注於聲音。

### 第二階段：迷霧揭示 (Mist Reveal)
* **觸發**：用戶點擊影片區域任何處。
* **畫面表現**：字幕以 **30% 透明度**（迷霧效果）浮現。
* **目的**：提供視覺輔助，但不過度干擾聽覺。

### 第三階段：單點解剖 (Sniper Loop) - **核心亮點**
* **觸發**：用戶點擊字幕中的任一單字。
* **即時反饋**：
    1. **影音跳轉**：影片與音訊瞬間跳回該單字的起始時間戳，播放該字發音（約 0.5-1 秒）後自動暫停。
    2. **視覺高亮**：該單字透明度變為 100% 並變色（高亮）。
    3. **專家氣泡**：若該單字符合「發音規律」字典，自動彈出簡短的提示氣泡（詳見 E.3）。

### 第四階段：主觀評分 (Self-Assessment)
* **選項**：卡片下方提供三個簡單按鈕。

| 按鈕 | 標籤 | 語意 |
| :--- | :--- | :--- |
| 🟢 **會了 (Easy)** | Easy | 聽力無障礙，清楚辨識 |
| 🟡 **模糊 (Hard)** | Hard | 能聽懂但需看字幕輔助 |
| 🔴 **重聽 (Again)** | Again | 完全跟不上語速 |

* **結果**：評分數據回傳至遺忘曲線 SRS 算法，決定下一次複習此影片段落的時間。

---

## E.3 專家級發音氣泡技能文檔 (Linguistic Popup Dictionary)

當用戶點擊特定單字時，系統會比對以下**「荷蘭語聽力魔鬼字典」**，並彈出對應的提示氣泡：

| 類別 (Category) | 觸發條件 (Trigger) | 氣泡內容 (Popup Content) |
| :--- | :--- | :--- |
| **連讀與弱化** | `is het`, `heb het`, `tot en met` | 💡 **連讀警告**：h 音常消失，字與字會黏在一起，聽起來像一個長音。 |
| **消失的結尾** | 動詞結尾 `-en` 或 `-nd` | 💡 **尾音弱化**：口語中結尾的 n 或 d 幾乎不發音，輕輕帶過。 |
| **時間陷阱** | `half` + 數字 | ⚠️ **魔王時間**：half negen 是 8:30 (九點的一半)，不是 9:30！ |
| **摩擦音辨識** | 包含 `g`, `ch`, `ui` 的單字 | 💡 **發音細節**：注意這個喉嚨摩擦音/特殊母音的震動位。 |
| **分離動詞** | 句末的 `in`, `op`, `af`, `uit` | 🔗 **分離動詞**：這與前面的動詞是一組的，要連起來理解。 |

---

## E.4 技術規格與資料結構 (Technical Specs)

### E.4.1 影音處理
* **格式**：H.264 MP4（確保影音同步的穩定性）。
* **託管**：Supabase Storage 或 AWS S3 CDN，確保毫秒級的 `Seek` 跳轉響應。

### E.4.2 資料格式 (JSON Data Schema)
每一段影片切片對應一個 JSON 物件，含字級時間戳：

```json
{
  "card_id": "NL_A2_EXAM_01",
  "video_url": "https://cdn.example.com/videos/scene_01.mp4",
  "segments": [
    {
      "word": "half",
      "start": 12.45,
      "end": 12.80,
      "popup_key": "dutch_time"
    },
    {
      "word": "negen",
      "start": 12.80,
      "end": 13.20,
      "popup_key": "dutch_time"
    }
  ]
}
```

### E.4.3 影片播放器核心功能
* `SeekTo(startTime)` — 跳轉至指定時間戳
* `PauseAfter(duration)` — 播放指定秒數後自動暫停
* `LoopSegment(start, end)` — 循環播放特定片段

---

## E.5 執行步驟 (Execution Roadmap)

| 階段 | 工作項目 | 說明 |
| :--- | :--- | :--- |
| **1. 內容整備** | 影片拆分 | 將影片切分為 30 個獨立段落（每段 10-15 秒） |
| **1. 內容整備** | 字級時間戳 | 使用 OpenAI Whisper 生成逐字時間戳 |
| **1. 內容整備** | 語言標籤 | 手動或透過規則引擎為每個字添加 Linguistic Popup 標籤 |
| **2. 播放器開發** | 影片控制組件 | 實作 SeekTo / PauseAfter / LoopSegment 功能 |
| **2. 播放器開發** | 迷霧字幕 UI | 實作三段式字幕狀態（隱藏 / 迷霧 30% / 完全顯示） |
| **3. 系統整合** | 新分頁封裝 | 將精聽實驗室封裝為 App 主導航新分頁 |
| **3. 系統整合** | SRS 對接 | 將評分邏輯（Easy/Hard/Again）寫入遺忘曲線資料庫 |

---




---

