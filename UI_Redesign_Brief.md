# Toocheepfordutch (MemoFlip) - 產品 UI/UX 重設設計師簡報

## 1. 產品概述 (Product Overview)
這是一款專為語言學習者打造的「AI 驅動沉浸式記憶與聽力訓練系統」。產品主要解決學習者在網路上閱讀文章、看影片時，遇到單字無法有效收集、複習，以及缺乏高效聽力訓練工具的痛點。

產品分為兩大端點：
1. **Toocheep Word Catcher (Chrome 擴充功能)**：負責在各大網頁收集單字、即時翻譯並送回雲端。
2. **MemoFlip Web App (主網頁應用)**：負責單字解析、間隔重複記憶 (SRS) 複習、文法解析以及精聽實驗室。

---

## 2. 核心架構與資料流 (Core Architecture & Data Flow)
使用者的典型學習路徑如下：
1. **收集 (Catch) - 新增單字流**：
   * **方法一：Chrome 擴充功能捕捉**：使用者在任何網頁閱讀時，反白選取不懂的單字，擴充功能會即時翻譯，並點擊收集按鈕將單字推送到資料庫的「雲端暫存區 (Inbox)」。
   * **方法二：手動匯入**：在 Web App 內點擊「匯入新單字」，直接貼上整段英文文章，由 AI 幫忙挑出重點單字。
2. **解析 (Parse) - Inbox 處理**：使用者進入 Web App 首頁，側邊欄會顯示所有從 Extension 抓過來的 Inbox 待辦單字清單。點擊「立刻解析」，一鍵透過 AI (OpenAI/Gemini) 將這些單字擴充為完整的單字卡（自動補齊音標、多詞性翻譯、雙語例句）。
3. **記憶 (Memorize) - 間隔重複**：進入 Review 模式，系統透過 SRS 演算法排程，將單字分為「總量池 (Pool)」、「大腦緩衝區 (Buffer)」與「已熟練 (Mastered)」，確保使用者不會被爆量的單字淹沒。
4. **應用 (Apply/Listen) - 精聽驗證**：進入「精聽實驗室 (Listening Lab)」進行高強度的影片聽寫與克漏字測驗，驗證聽力。

---

## 3. Web App 核心頁面與功能細節 (Pages & Features)

### A. 首頁儀表板 (Dashboard / Home Page)
這是使用者每次打開 App 的入口，設計重點在於「提供明確的學習進度與下一步行動」。
* **動態問候語**：依據時間顯示「早安/午安/晚安」。
* **學習漏斗視覺化 (Memory Funnel)**：
  * **Layer 1: 總量池 (The Pool)** - 收集進來但尚未開始背誦的新單字總數。
  * **Layer 2: 大腦緩衝區 (Buffer)** - 正在記憶中、或是剛忘記正在重學的單字。有容量上限（預設 50，表現好會自動升級到 120），分為「新詞背誦」與「記憶修復」。會有進度條顯示滿載程度。
  * **Layer 3: 已熟練 (Mastered)** - 已經進入長期記憶的字，並顯示「今日到期需複習」的數量。
* **行動呼籲 (CTA) 按鈕**：
  * 開始複習（依據緩衝區狀態，動態顯示每次 20/30/40 張）。
  * 匯入新單字（手動貼上文字請 AI 幫忙轉換）。
  * 處理 Inbox（若 Extension 有傳單字進來，會出現提示按鈕）。
* **需要加強 (Weak Cards) 便利貼**：在側邊欄以「便利貼」視覺顯示使用者一直背不起來的字，可手動隱藏。
* **網頁收集 (Inbox) 列表**：在側邊欄顯示從 Extension 送過來的單字清單。

### B. 單字複習模式 (Review Mode)
沉浸式的翻卡測驗介面。
* **卡片正面**：顯示單字。
* **卡片背面**：顯示音標、多詞性翻譯、兩個例句（含中文翻譯）、以及使用者自訂筆記 (User Notes)。
* **評分按鈕與快捷鍵 (Shortcuts)**：翻開背面後，提供四個選項讓 SRS 演算法計算下次出現時間。為了達到沉浸式「心流」，系統全面支援鍵盤盲操：
  * `空白鍵 (Space)`：翻開卡片查看答案 / 翻回正面。
  * `Q 鍵`：選擇「完全沒印象 (Again)」。
  * `W 鍵`：選擇「有點模糊 (Hard)」。
  * `E 鍵`：選擇「記得了 (Good)」。
  * `R 鍵`：選擇「輕鬆秒殺 (Easy)」。
* **狀態與回饋**：滑鼠移至評分按鈕或使用快捷鍵時，按鈕上方會預覽「XX 分鐘/天後再複習」。
* **心流控制**：結束一次複習 (Session) 後，系統會計算正確率，若正確率 > 90% 連續三次，會提示進入「加速模式」擴大緩衝區；若正確率過低，會主動幫忙降載。

### C. 精聽實驗室 (Listening Lab)
針對高階學習者的影音聽寫訓練區，需設計為全螢幕、具備沉浸感的介面。
* **影片播放器**：目前嵌入 YouTube 影片。
* **章節導航 (Chapters)**：側邊欄列出所有句子/章節，點擊可直接跳轉影片秒數。
* **克漏字測驗 (Cloze Test)**：
  * 難度切換：提供 Low (挖空 15%)、Mid (40%)、High (75%)。
  * 互動機制：滑鼠移到空格上 (hover) 會浮現答案，**同時影片會自動暫停**；移開後影片自動繼續播放。
* **快捷鍵操作 (Shortcuts)**：讓學習者雙手不離開鍵盤，專注盲聽。
  * `空白鍵 (Space)`：暫停 / 繼續播放。
  * `A 鍵`：回到當前句子的開頭（如果連按兩次則跳回上一句）。
  * `D 鍵`：直接跳轉到下一句。
* **UI 狀態切換**：右下角提供「顯示/隱藏原文字幕」的眼睛按鈕。

### D. 卡片庫 (Card Library)
傳統的資料表介面，供使用者管理所有資料。
* **列表清單**：顯示所有卡片，支援搜尋功能。
* **編輯功能**：可以直接在此刪除卡片。

### E. 文法解析 (Grammar View)
* 提供文字輸入框，使用者貼上長難句，透過 AI 拆解文法結構與句型分析。

---

## 4. 給設計師的 UI/UX 重設建議 (Design Directives)

1. **專業與現代感 (Modern & Premium Aesthetic)**：
   * 目前產品已具備強大的底層邏輯，UI 需要從「工程師的 MVP」升級為「SaaS 等級的專業產品」。
   * 建議參考 Linear, Notion, 或 Duolingo 的俐落感，但保留專業大人的學習風格。
   * 色彩：建議採用柔和的淺色底 (Light Mode) 搭配高對比度的重點色（目前使用橘/藍/紅/綠作為狀態色），並可考慮設計 Dark Mode。
   
2. **核心視覺：漏斗與卡片 (Funnel & Cards)**：
   * **首頁的漏斗 (Funnel)** 是產品最核心的學習哲學。設計師可以嘗試將「池子 -> 緩衝區 -> 熟練」這三個階段，用更具象或更具科技感的資訊圖表 (Infographic) 呈現，讓使用者一眼看出大腦目前的負載狀況。
   * **複習卡片 (Review Card)** 需要極致的易讀性。排版要有層次：單字最大，音標與詞性次之，例句要有足夠的留白 (White space)。

3. **精聽實驗室 (Listening Lab) 的沉浸感**：
   * 這是目前最吃重互動的頁面。影片、字幕、克漏字測驗、側邊欄導航，需要一個乾淨的分屏設計。
   * 克漏字 (Cloze) 的「懸停顯示答案」需要明顯的視覺提示（例如：底線樣式、發光效果）。

4. **響應式設計 (Responsive Design)**：
   * 手機版 (Mobile) 時，Review 頁面需要支援單手操作（例如按鈕夠大、或支援手勢滑動），首頁的便利貼與 Inbox 會自動隱藏或摺疊。

---

## 5. 系統狀態與設定 (Settings & Sync)
這部分不影響主視覺，但包含在設定彈窗中：
* **AI API Key**：使用者需填入自己的 OpenAI 或 Gemini Key。
* **同步機制**：輸入 Sync ID 進行 Supabase 雲端資料同步。
* **資料匯出/還原**：JSON 備份下載。
